---
name: leyoujia-main-image
description: 生成乐优家电商主图：给定一张干净产品图、一句话卖点和3个小卖点，一次输出天猫1440×1440、天猫1440×1920、唯品会1440×1440、唯品会950×1200四种规格。天猫版套左上角乐优家半弧Logo，唯品会版不叠Logo并控制到800–999KB。用于主图复刻/套版，不用于详情页长图、普通抠图或产品重绘。
---

# 乐优家主图套版

从一张干净产品图生成下列四张主图，保持产品原样，只做等比裁切、文字排版和平台Logo叠加。

标准归档目录：`D:\HuaweiMoveData\Users\HUAWEI\Documents\电商平台\乐优家主图套版`。

完整流程、2026-09-12验证记录和后续测试约定见 `references/workflow.md`：

| 输出 | 尺寸 | 左上角 | 文件限制 |
|---|---|---|---|
| `天猫主图-1440.jpg` | 1440×1440 | 乐优家半弧白底+Logo | 不限 |
| `天猫3比4主图.jpg` | 1440×1920 | 乐优家半弧白底+Logo | 不限 |
| `唯品会主图-1440.jpg` | 1440×1440 | 无 | 800–999KB |
| `唯品会主图950.jpg` | 950×1200 | 无 | 800–999KB |

## 输入要求

用户需要提供：

1. 一张**干净产品图**：没有旧文案、旧Logo，也不要把已经排好字的成品拿来二次叠字。
2. 一句话主卖点，例如 `醇柔磨毛床品四件套`。
3. 正好3个小字卖点，用 `|`、`｜`、逗号或顿号分隔，例如 `全棉磨毛|数码印花|不易掉毛`。

缺任一项时先用一句话补齐，不要猜产品卖点。若原图带旧文案，先请用户提供干净图；不要用 AI 重绘产品。


## 已校准排版

- 第一行：梦源宋体 Exp CN W12，颜色 `#5a2f01`，居中。
- 第二行：梦源宋体 Exp CN W8，颜色 `#5a2f01`，居中；三个卖点之间留间距，并绘制同色竖线。
- 字体颜色、字号、文字位置和Logo位置为默认固定规则，不主动修改。
- Photoshop 文字引擎字号为 55pt / 37.5pt，成品通过图层变换缩放；完整测量见 `references/psd-layout.md`。
- 四种成品的实际渲染字号：1:1 为 100/67px，3:4 为 107/72px，950×1200 为 71/48px；分割线按 PSD 中的独立矢量矩形比例绘制。
- 字体来源：`https://github.com/Pal3love/dream-han-cjk`，SIL Open Font License，许可证见 `LICENSE-DreamHanSerif.txt`。

## 执行

使用 `scripts/compose_main_images.py`：

```powershell
python scripts/compose_main_images.py "<干净产品图>" `
  --title "<一句话主卖点>" `
  --points "<卖点1>|<卖点2>|<卖点3>" `
  --out "<输出目录>" `
  --manifest
```

可选参数：

- 1:1 默认 `focus=(0.5,0.65)`，向下取景保留产品底部。
- 3:4 和950×1200默认 `focus=(0.5,1.0)`、`crop_zoom=0.90`，减少顶部墙面并适度放大产品，使产品尽量居中。
- `--focus x,y` 可临时覆盖默认取景中心。
- `--title-font "<字体文件>"`：默认使用已内置的原始字体 `assets/DreamHanSerifExpCN-W12.ttf`。
- `--points-font "<字体文件>"`：默认使用已内置的原始字体 `assets/DreamHanSerifExpCN-W8.ttf`。
- `--font "<字体文件>"`：临时同时覆盖两行字体。
- `--text-color "#RRGGBB"`：仅按用户要求临时覆盖字体颜色，默认仍为 `#5a2f01`。
- `--shadow-color "#RRGGBBAA"`：随临时换色一起增加柔和文字阴影，例如 `#4A2A18C0`。

脚本会按画布等比 cover 裁切，不拉伸产品；1:1 默认向下取景，3:4和950×1200默认缩放至90%裁切窗口以优先展示完整床品。主卖点和小卖点固定字号、单行显示，不自动换行、不自动缩小；如需调整由用户明确提出。天猫两张固定质量 100；唯品会两张通过二分查找画质，严格保持在 819,200–999,000 字节。若最高画质仍低于 819,200 字节，使用标准 JPEG 注释段补足文件大小，不改变图像像素。

## 交付检查

1. 检查四张尺寸分别为 1440×1440、1440×1920、1440×1440、950×1200。
2. 天猫两张必须有左上角半弧白底+乐优家Logo，唯品会两张必须完全没有Logo/白弧。
3. 检查主卖点和小卖点无缺字、方框、乱码；分隔符应显示为棕色竖线。
4. 检查产品主体没有被裁切异常；需要时用 `--focus` 重跑。
5. 检查唯品会两张文件大小均在819,200–999,000字节之间。
6. 打开四张成品给用户预览，并报告输出目录。
7. 交付最后必须询问用户：是否存在遮挡？是否需要更换字体颜色？未经用户确认，不自行改变Logo位置、字体颜色、字号或文字位置。

## 非范围

- 不做详情页长图、商品抠图、背景合成或产品重绘。
- 不修改产品花型、颜色、纹理、褶皱或结构。
- 用户没有提供干净原图时，不基于已排版成品直接出图。













